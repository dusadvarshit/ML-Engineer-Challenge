# Checklist

- Later: if the API needs to serve multiple models from the registry, add a registry-aware loading approach with explicit model/version selection, lazy loading, and in-memory caching. Do not introduce a model manager yet.
